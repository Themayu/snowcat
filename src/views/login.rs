use crate::App;
use crate::views::LoginScreenState;
use dominator::Dom;
use std::rc::Rc;
use std::sync::Arc;

pub fn login_view(app: Arc<App>, state: Rc<LoginScreenState>) -> Dom {
	unimplemented!()
}
